"use client"

export default function RunsPage() {
  return (
    <div>
      <h1>All Runs</h1>
      {/* Global runs list - cross-task search/filter functionality */}
      {/* Can filter by status, date, pipeline, task, duration, etc. */}
    </div>
  )
}
