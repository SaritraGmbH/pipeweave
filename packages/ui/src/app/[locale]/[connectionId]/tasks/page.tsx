"use client"

export default function TasksPage() {
  return (
    <div>
      <h1>All Tasks</h1>
      {/* Global tasks list - cross-pipeline search/filter functionality */}
      {/* Can filter by pipeline, service, status, assigned user, etc. */}
    </div>
  )
}
