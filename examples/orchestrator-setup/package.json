{
  "name": "orchestrator-setup-example",
  "version": "0.1.1",
  "description": "Example demonstrating how to set up and use the PipeWeave orchestrator",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "start:serverless": "MODE=serverless tsx src/server.ts",
    "start:env": "tsx src/server-from-env.ts"
  },
  "dependencies": {
    "@pipeweave/orchestrator": "workspace:*",
    "@pipeweave/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
